import styled from 'styled-components'

export const Wrapper = styled.div`
  position: relative;
  display: flex;
  gap: 1rem;

  margin: 0 6rem;
  width: 120rem;
  height: auto;
  overflow: hidden;
`;

import styled from 'styled-components'

export const Wrapper = styled.div`
  display: flex;
  gap: 1rem;

  margin: 0 6rem;
  width: 120rem;
  min-height: 25rem;
  height: auto;
  overflow-x: scroll;
  overflow-y: hidden;
`;
